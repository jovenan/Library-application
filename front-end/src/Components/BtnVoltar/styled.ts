import styled from "styled-components";

export const BtnVoltarDiv = styled.div`
    input {
        background: 0;
        border: 0;
        font-size: 2.5rem;
        position: absolute;
        left: 30%;
        color: #fff;
        cursor: pointer;
    }
`